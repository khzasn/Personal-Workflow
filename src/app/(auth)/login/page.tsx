import { LoginForm } from "@/features/auth/login-form";

export const metadata = {
  title: "Masuk | Dayflow",
  description: "Masuk ke Dayflow dan lanjutkan rencana produktifmu.",
};

export default function LoginPage() {
  return <LoginForm />;
}
